name: keepalive
on:
  schedule:
    - cron: "23 4 */3 * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -fsS --retry 3 --retry-delay 5 -X POST \
            "https://xwnjimsoframcqstdjfo.supabase.co/rest/v1/rpc/keepalive_ping" \
            -H "apikey: sb_publishable_GxJNb-tD7jTgxskyPpgVLQ_ZNJZ9mm3" \
            -H "Content-Type: application/json" -d '{}'
